{
  "name": "portail-support",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Portail support local pour pratiquer Claude Code",
  "scripts": {
    "start": "node backend/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
